# EventSeries { #natural_features.EventSeries }

```python
EventSeries(
    onset_s,
    offset_s,
    label=None,
    confidence=None,
    extra=dict(),
    metadata=dict(),
    schema='EventSeries/v1',
    timebase=(lambda: TimebaseSpec(kind='events'))(),
)
```
